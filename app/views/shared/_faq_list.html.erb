  <table class='mtop'>
    <tbody>
        <%= render partial: 'shared/faq_row', collection: @faqs, as: :faq %>
    </tbody>
  </table>

<div id='faq-list' class="nav pull-right">
  <%= will_paginate @faqs if @faqs.respond_to?(:total_pages) %>
</div> 
