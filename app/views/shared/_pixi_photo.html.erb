<fieldset>
  <td>
  <% if !f.object.new_record? %>
    <%= image_tag f.object.photo.url %>
  <% end %>

  <%= f.file_field :photo, { id: "photo", multiple: true } %>
  <%= delete_photo(f) %></td>
</fieldset>
