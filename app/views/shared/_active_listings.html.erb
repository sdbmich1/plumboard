<%= f.collection_select(:pixi_id, model.active_listings.reorder('title ASC'), :pixi_id, :title, 
  {selected: f.object.pixi_id, include_blank: 'Select Pixi'}, { id: 'pixi_id1', class: 'pixi-select' }) %>
