<div class='mtop nav pull-right'>
  <%= link_to 'Back', pending_listings_path, class: 'btn btn-large' %>
  <div class="btn-group">
    <button type="button" class="btn btn-large dropdown-toggle" data-toggle="dropdown">
      Deny <span class="caret icon-white"></span>
    </button>
    <ul class="dropdown-menu" role="menu">
      <li><%= link_to 'Improper Content', deny_pending_listing_path(listing, reason: 'Improper Content'), method: :put %></li>
      <li><%= link_to 'Bad Pictures', deny_pending_listing_path(listing, reason: 'Bad Pictures'), method: :put %></li> 
    </ul>
  </div>
  <%= link_to 'Approve', approve_pending_listing_path(listing), method: :put, class: 'btn btn-large btn-primary submit-btn' %>
</div>

