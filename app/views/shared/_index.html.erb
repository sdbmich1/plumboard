<% provide(:title, page_title) %>
<%= content_for :content do %>
  <h2><%= page_title %></h2>
  <%= link_to 'Add Pixi', new_listing_path(:user_id=>@user) %>
  <%= render partial: 'shared/listing', collection: @listings %>
<% end %>

