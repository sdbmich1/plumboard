<% if model %>
  <div class='center-wrapper'>
    <div class="bxslider">
      <% for pic in model.pictures %>
        <div class='slide'>
          <%= image_tag get_pixi_image(pic, set_element(true)), class: 'lrg_pic_frame', title: set_element(false) if pic.photo? %>
        </div>
      <% end %>
    </div>
  </div>
<% end %>
