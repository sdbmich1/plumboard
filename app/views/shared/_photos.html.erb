<% if model %>
    <% for pic in model.pictures %>
      <div class='pic-frame'>
        <% unless pic.photo_file_name.blank? %>
	  <%= image_tag pic.photo.url, :size => psize %>
        <% end %>
      </div>	
    <% end %>
<% end %>
