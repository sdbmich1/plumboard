<table>
   <tr>
    <th>Item</th>
    <th>Quantity</th>
    <th>Total</th>
  </tr>
  <% txn.transaction_details.each do |item| %>
    <%= show_txn_item item %>
  <% end %>
  <%= render partial: 'shared/order_total', :locals=>{paid: paid, txn: txn, model: invoice} %>
</table>
