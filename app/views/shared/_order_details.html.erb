<tr>
  <th class='width320'>Item</th>
  <th class="price width120">Quantity</th>
  <th class="price width140">Total</th>
</tr>
<% (1..order[:cnt].to_i).each do |i| %>
  <%= process_order_row i, order %>
<% end %>
<%= render partial: 'shared/order_total', locals: {txn: txn, model: RecursiveOpenStruct.new(order), paid: paid, order: order} %>
