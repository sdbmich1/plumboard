<% if model %>
  <% for pic in model.pictures %>
      <% unless pic.id.blank? %>
	<%= image_tag pic.photo.url, :size => psize %>
	<%= link_to 'Remove', picture_path(pic, pixi_id: model.pixi_id), method: :delete, confirm: 'Image will be removed. Are you sure?', remote: true %>
      <% end %>
  <% end %>
<% end %>
