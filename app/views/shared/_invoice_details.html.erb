  <table class="mtop table table-bordered">
  <%= render partial: 'shared/inv_header', locals: { colFlg: false } %>
  <%= render partial: 'shared/invoice_items', collection: invoice.invoice_details, as: :invoice %>
  <tr>
    <td></td>
    <td><div class='price'>Sales Tax</div></td>
    <td class="width120"><div class='price'><%= get_sales_tax(invoice) %>%</div></td>
    <td class="width120"><div class='price'><%= ntc(invoice.tax_total) %></div></td>
  </tr>
  <tr>
    <td></td>
    <td><div class='price'>Shipping</div></td>
    <td></td>
    <td class="width120"><div class='price'><%= ntc(invoice.ship_amt) %></div></td>
  </tr>
  <%= render partial: 'shared/show_conv_fee', locals: {showColFlg: true, invoice: invoice} unless invoice.owner?(@user) %>
  <tr>
    <td></td>
    <td></td>
    <td><div class='order-total price'>Amount Due</div></td>
    <td class="width120"><div class='order-total price'><%= ntc get_invoice_total(invoice) %></div></td>
  </tr>
  <%= render partial: 'shared/seller_invoice_details', locals: {invoice: invoice} if invoice.owner?(@user) %>
  </table>
</div>

