<% provide(:title, @usr.name) %>
<%= content_for :subhead do %>
  <%= render :partial => 'shared/navbar', locals: { menu_name: 'Users' } %>
<% end %>
<%= content_for :content do %>
  <div class="row mbot" id="user_form">
    <%= render partial: 'shared/show_user', locals: { user: @usr } %>
  </div>
<% end %>

