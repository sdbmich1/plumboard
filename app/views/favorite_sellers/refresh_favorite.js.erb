$('#spinner').show('fast');
<% if @favorite.errors.any? %>
  $("#form_errors").html("<%= escape_javascript( render(partial: 'shared/error_messages', locals: {target: @favorite})) %>");
  $("#flash_notice").hide(300);
<% else %>
  $("#flash_notice").html('');
  postFlashMsg('#flash_notice', 'success', 'Succesfully #{atype} seller');
  $("#usr-panel").html("<%= escape_javascript( render(partial: 'shared/user_details', locals: {listing: @favorite.seller}) ) %>");
<% end %>
$('#spinner').hide(300);

