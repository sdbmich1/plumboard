<%= content_for :panel do %>
  <%= render 'mobile/left_panel' %>
<% end %>

<%= content_for :head do %>
  <%= render partial: 'layouts/header', locals: { psize: 'Build Your Pixi' } %>
<% end %>

<%= content_for :content do %>
  <div id="buyTxtForm">
    <div id="buyFormError" style="display:none" class="error"></div>
    <%= render partial: 'mobile/listing_form', locals: { step_name: 'Build Your Pixi', action_type: 'Save' } %>
  </div>
<% end %>  
