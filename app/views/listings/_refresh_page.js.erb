<%= render partial: 'shared/refresh_menu', locals: { rFlg: rFlg, btype: btype, loc_name: loc_name, model: model } %>

// reload board
<%= render partial: 'shared/reload_board', locals: {model: model} %>
