$("#pxboard").html("<%= escape_javascript(render(partial: 'shared/listings')) %>");

// reload board
reload_board(this);

<% unless @listings.next_page %>
  $('a.nxt-pg').remove();
<% end %>

$('#nearby-map').hide('fast');
$('#spinner').hide(300);
