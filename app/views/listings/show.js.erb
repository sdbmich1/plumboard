$('.posts').html("<%= escape_javascript( render(partial: 'shared/comment', collection: @comments)) %>");
$('#comment-list').html('<%= escape_javascript(will_paginate @comments, page_links: false) %>');
$('#spinner').hide(300);
