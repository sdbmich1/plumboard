$('.posts').html("<%= escape_javascript( render(partial: 'shared/comment', collection: @listing.comments)) %>");
$('#comment-list').html('<%= escape_javascript(will_paginate @listing.comments, page_links: false) %>');
$('#spinner').hide(300);
