$("#inv_price").val("<%= @price %>");
$("#inv_buyer_select").html("<%= escape_javascript( raw render(:partial => 'shared/pixi_wanted_list', locals: {pid: @listing.pixi_id}) ) %>");

// recalculate totals
calc_amt();

$("#spinner").hide('fast'); 
