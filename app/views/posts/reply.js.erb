<% if @post.errors.any? %>
  $("#notice").hide(300);
  $("#form_errors").html("<%= escape_javascript( render(partial: 'shared/error_messages', locals: {target: @post})) %>");
<% else %>
  $("#notice").html("<%= escape_javascript(flash[:notice])%>");
  $("#notice").show(300);
  $("#post-frm").html("<%= escape_javascript( raw render(partial: "shared/posts", locals: { sentFlg: false }) ) %>");
<% end %>
$("#spinner").hide('fast'); 

