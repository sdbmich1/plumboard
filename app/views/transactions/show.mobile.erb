<%= content_for :panel do %>
  <%= render 'mobile/left_panel' %>
<% end %>

<%= content_for :head do %>
  <%= render partial: 'layouts/header', locals: { psize: "#{get_page_header} Complete"} %>
<% end %>

<%= content_for :content do %>
  <div id="buyTxtForm" class='neg-top'>
    <%= render partial: set_txn_partial, locals: {step_name: "#{get_page_header} Complete"} %>
  </div>
<% end %>

